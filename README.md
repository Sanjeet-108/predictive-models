# Stock Market Prediction (LSTM + Streamlit)

Fetches Yahoo Finance data, trains an LSTM on closing prices, and visualizes MAs and predictions.

## Setup
pip install -r requirements.txt

## Run
streamlit run app.py
